.DS_Store
.env

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.backfill_cursor
# Raw polymarket/kalshi parquet dataset (~50 GB after extraction).
# Anchored with leading `/` so `our_code/data/` (committed wallet CSVs) is NOT ignored.
/data/
data.zip

research/*
!research/main.tex
!research/main.pdf
!research/fig
!research/analysis
!research/sections

scratchpad

fig
*.tar.zst
output
.tmp

# Anchored at repo root; our_code/results/ is intentionally committed.
/results/
.ipynb_checkpoints/

# `our_code/data/` and `our_code/results/` are intentionally committed so the
# Streamlit app + notebook run standalone from a fresh clone.  Raw parquet data
# lives in `data/` (above) which stays ignored.

# Claude Code session state
.claude/
